# Force a single postcss instance across the dep tree. Stylelint's custom syntax
# (postcss-styled-jsx via @dhis2/cli-style) and stylelint itself must share the
# same postcss, otherwise documents parsed by one are unintelligible to the other.
# Install the current host's native binaries (os/cpu/libc) plus Linux glibc, so the same
# node_modules also serves the Linux dev container that shares it. `current` must stay in
# every list, or the host/CI loses its own arch/libc. On a Linux host the container matches
# the host (current covers it); on macOS the container is Linux, so the explicit `linux` +
# arm64 + glibc entries cover it.
supportedArchitectures:
  os:
    - current
    - linux
  cpu:
    - current
    - arm64
  libc:
    - current
    - glibc
overrides:
  postcss: ^8.5.15
allowBuilds:
  '@dhis2/cli-helpers-engine': true
  '@dhis2/cli-style': true
  '@parcel/watcher': true
  core-js: true
  core-js-pure: true
  cypress: true
  esbuild: true
  opensrc: true
minimumReleaseAge: 4320
minimumReleaseAgeExclude:
  - '@dhis2/*'
  - '@dhis2-ui/*'
onlyBuiltDependencies:
  - '@dhis2/cli-helpers-engine'
  - '@dhis2/cli-style'
  - '@parcel/watcher'
  - core-js
  - core-js-pure
  - cypress
  - esbuild
  - opensrc
  - unrs-resolver
# These hoists are needed for @dhis2/app-shell since it uses directly some libraries that were hoisted by yarn 1
# This is a better alternative than pnpm's shamefullyHoist https://pnpm.io/settings#shamefullyhoist until we update the way that @dhis2/app-shell works
publicHoistPattern:
  - '@dhis2/ui'
  - 'typeface-roboto'
  - '@dhis2/app-adapter'
  - 'prop-types'
  - '@dhis2/d2-i18n'
  - 'post-robot'
  - 'styled-jsx'
  - '@dhis2/pwa'
